services:
    front-nginx:
        networks:
            - user-network
        build:
            context: nginx
            dockerfile: Dockerfile
        ports:
            - "443:443"
            - "80:80"
        volumes:
            - frontend_data:/frontend/
            - ssl_volume:/etc/nginx/ssl
            - common:/share-resource
