services:
  pong-game:
    networks:
        - user-network
    build:
        context: .
        dockerfile: Dockerfile
  redis:
    networks:
      - user-network
    build:
      context: redis
      dockerfile: Dockerfile